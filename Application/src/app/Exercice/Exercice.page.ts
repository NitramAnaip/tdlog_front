import { Component } from '@angular/core';

@Component({
  selector: 'app-Exercice',
  templateUrl: 'Exercice.page.html',
  styleUrls: ['../style.scss'],
})


export class ExercicePage {
  /*private constructor () {
    const resultat = document.getElementById('resultat');
    resultat.innerHTML = '<p>dfjkdjfhg</p>';
  }

  button = document.getElementById('bouton');
  champ = document.getElementById('champ');
  resultat = document.getElementById('resultat');
  nTentatives = 0;
  private vraieTraduction = 'salut'; // emplacement de la bonne réponse
  private click() {
    this.nTentatives++;
    const valeur = this.champ.value;
    if (valeur !== this.vraieTraduction) {
      if (this.nTentatives <= 3) {
        this.resultat.innerHTML = '<p>dfjkdjfhg</p>'; // conseil a donner quand il se trompe
        alert('plus que ' + (3 - this.nTentatives) + ' tentatives');
      } else {
        alert('déso c\'est fini');
      }
    } else {
      alert('bravo');
    }
  */
  // this.button.addEventListener('click', click);
}
